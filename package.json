{
  "name": "tf-idf-monorepo",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1",
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/express": "^5.0.0",
    "@types/cors": "^2.8.17",
    "@types/body-parser": "^1.19.5",
    "typescript": "^5.3.3"
  }
}
